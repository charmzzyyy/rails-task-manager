<%= form_for(task) do |f| %>
  <%= f.label :Title %>
  <%= f.text_field :title %>

  <%= f.label :Details %>
  <%= f.text_field :details %>

<% if task.persisted? %>
  <%= f.label :completed %>
  <%= f.check_box :completed %>
  <% end %>

  <%= f.submit %>

<% end %>
